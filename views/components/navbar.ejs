<nav>
  <% links.forEach(link => { %>
  <div class="links">
    <a href="<%= link.href %>"
      ><%= link.text %> <% if (link.icon) { %>
      <i class="<%= link.icon %>"></i>
      <% } %></a
    >
  </div>
  <% }) %>
</nav>
